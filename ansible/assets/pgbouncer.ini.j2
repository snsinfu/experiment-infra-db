[databases]
* =

[pgbouncer]
listen_addr = *
listen_port = 6432
pidfile = /var/run/pgbouncer/pgbouncer.pid

pool_mode = session
max_client_conn = 500
default_pool_size = 5

auth_file = /usr/local/etc/pgbouncer.users
auth_type = hba
auth_hba_file = /usr/local/etc/pgbouncer.hba
