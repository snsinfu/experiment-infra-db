server_hostname = "{{ infra.server_hostname }}"
server_location = "{{ infra.server_location }}"
server_type = "{{ infra.server_type }}"
server_image = "{{ infra.server_image }}"
volume_id = "{{ persistent_volume_id }}"

admin_user = "{{ admin_user }}"
admin_password_hash = "{{ admin_password | password_hash("sha512", admin_password_salt) }}"
admin_public_keys = {{ admin_public_keys | to_json }}
